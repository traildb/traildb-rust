language: rust
rust:
  - stable
  - nightly

os:
  - linux

dist: bionic

env:
  global:
    - RUST_BACKTRACE=1

addons:
  apt:
    packages:
      - clang
      - libjudy-dev
      - libarchive-dev
      - pkg-config
      - build-essential
      - m4